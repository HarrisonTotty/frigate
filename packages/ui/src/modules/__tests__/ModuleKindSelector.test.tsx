// ModuleKindSelector tests are intentionally removed.
// This component will be replaced by the ModuleCatalog flow per the ship-design workspace action plan.

import { describe } from 'vitest';

// keep a skipped suite so the test runner doesn't error on an empty file
describe.skip('ModuleKindSelector (removed)', () => {});

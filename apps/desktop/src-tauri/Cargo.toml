[package]
name = "frigate-desktop"
version = "0.1.0"
description = "Frigate desktop shell"
license = "MIT"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tauri = { version = "2.0.0-alpha.14", features = ["protocol-asset", "default"] }

[build-dependencies]
tauri-build = { version = "2.0.0-alpha.7", features = [] }
